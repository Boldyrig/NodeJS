const CONFIG = {
    PORT: 3000,

    // список всех триггеров в системе
    TRIGGERS: {
        SQR: 'SQR'
    },

    // список всех событий в системе
    EVENTS: {
        EV1: 'ev1',
        EV2: 'ev2'
    }
};

module.exports = CONFIG;